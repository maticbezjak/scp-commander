# Rust
/target
**/*.rs.bk

# macOS
.DS_Store
# Repo-root packaging output; NOT ui-tauri/dist which is the Tauri frontend
# and must be committed.
/dist/
# Tauri's generated schema/permission files.
ui-tauri/src-tauri/gen/
# Tauri/Svelte frontend build output + deps (built in CI via `npm run build`).
ui-tauri/dist/
ui-tauri/node_modules/
